// ---------------------------------------------------------------------------
// Convenience re-export so consumers can import from the hooks barrel.
// ---------------------------------------------------------------------------

export { useAuthStore } from '@/stores/auth';
export type { AuthUser, RegisterPayload } from '@/stores/auth';
